# Edge-NET DHCP server
# Fixed addresses assigned by MAC — nodes get predictable IPs
# All nodes connect via WiFi (bwfm0); ethernet is optional home network uplink only

subnet 10.1.1.0 netmask 255.255.255.0 {
    option routers 10.1.1.1;
    option domain-name-servers 8.8.8.8;
    range 10.1.1.100 10.1.1.199;

    host edge-net-keybow {
        hardware ethernet b8:27:eb:28:2d:8e;
        fixed-address 10.1.1.10;
    }

    host edge-net-automation {
        hardware ethernet b8:27:eb:b0:19:1b;
        fixed-address 10.1.1.12;
    }
}
